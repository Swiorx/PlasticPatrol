version: '3.8'

services:
  db:
    image: postgis/postgis:15-3.3
    container_name: plasticpatrol_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: plasticpatrol
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
